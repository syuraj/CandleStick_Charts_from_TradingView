<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container2"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.siristechnology.candlestick.AddRemoveStockActivity"
    tools:ignore="MergeRootFrame">

    <ListView
        android:id="@android:id/list"
        style="@style/ReaderListView.PostList"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:dividerHeight="@dimen/margin_large"
        tools:listitem="@layout/news_item"></ListView>

    <TextView
        android:id="@id/android:empty"
        style="@style/ReaderListView.EmptyList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="@string/empty_stocks_msg" />
</FrameLayout>
